<?php

function config__record_column_edit($name,$value,$options)
{
	//just print stuff out, it's captured by the output buffer
	
	//if we have an override to the core - return true to stop processing, not common!
}